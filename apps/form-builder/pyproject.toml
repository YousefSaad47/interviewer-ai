[project]
name = "form-builder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = true

[project.scripts]
form-builder = "main:main"

[dependency-groups]
dev = ["ruff>=0.15.16", "ty>=0.0.47"]
