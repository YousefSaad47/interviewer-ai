datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "../src/generated"
}

model User {
  id           String   @id @default(uuid()) @db.Uuid()
  email        String   @unique

  @@map("users")
}
